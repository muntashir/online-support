extends layout

block styles
    link(rel='stylesheet', href='/stylesheets/lobby.css')

block scripts
    script.
        sessionID="#{sessionID}";
        company="#{company}";
        prompt="#{prompt}";
    script(src='/javascripts/lobby.js')

block content
    nav.navbar.navbar-default.navbar-fixed-top
        .container-fluid
            .navbar-header
                a.navbar-brand(href="/") #{title}
    #window
        .well
            button.btn.btn-default.button(id="request") Request chat